# 🚀 Vite 开发环境设置

本项目已从 Python HTTP 服务器迁移到 **Vite** 开发服务器。

## 📋 前置要求

- Node.js 16+ 
- npm 或 yarn 或 pnpm

## 🔧 安装依赖

```bash
cd src/claprun
npm install
```

## 🎮 开发模式

启动 Vite 开发服务器：

```bash
npm run dev
```

浏览器会自动打开 `http://localhost:5173`

**特性**:
- ✅ 热模块替换 (HMR) - 代码改动自动刷新
- ✅ 快速启动
- ✅ 优化的构建

## 📦 生产构建

构建优化的生产版本：

```bash
npm run build
```

输出文件在 `dist/` 目录中

## 👀 预览生产构建

```bash
npm run preview
```

## 📁 项目结构

```
src/claprun/
├── index.html              # 主 HTML 文件
├── js/
│   ├── main.js            # 主程序入口
│   ├── audio-recognizer.js # 音频识别模块
│   └── game.js            # 游戏引擎模块
├── package.json           # 项目配置
├── vite.config.js         # Vite 配置
└── dist/                  # 生产构建输出（构建后生成）
```

## 🔄 从 Python 服务器迁移

### 删除的文件
- ❌ `server.py` - Python HTTP 服务器
- ❌ `start.sh` - macOS/Linux 启动脚本
- ❌ `start.bat` - Windows 启动脚本

### 新增的文件
- ✅ `package.json` - Node.js 项目配置
- ✅ `vite.config.js` - Vite 构建配置
- ✅ `.gitignore` - Git 忽略文件

### 代码改动
- ✅ `index.html` - 改为 Vite 模块格式
- ✅ `js/main.js` - 添加 ES6 模块导入
- ✅ `js/audio-recognizer.js` - 添加导出
- ✅ `js/game.js` - 添加导出

## 🌐 部署

### 静态托管（推荐）

将 `dist/` 目录部署到任何静态托管服务：

- Vercel
- Netlify
- GitHub Pages
- AWS S3
- 任何 CDN

### 示例：Vercel

```bash
npm install -g vercel
vercel
```

### 示例：Netlify

```bash
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

## 🐛 调试

### 开发工具

Vite 提供完整的源代码映射，便于调试：

1. 打开浏览器开发者工具 (F12)
2. 查看 Sources 标签页
3. 代码改动会自动刷新

### 控制台日志

所有日志前缀：
- `[Main]` - 主程序日志
- `[AudioRecognizer]` - 音频识别日志
- `[Game]` - 游戏引擎日志

## 📝 常见问题

### Q: 端口 5173 已被占用

编辑 `vite.config.js`：
```javascript
server: {
    port: 3000,  // 改为其他端口
}
```

### Q: 如何在生产环境中使用

1. 运行 `npm run build`
2. 部署 `dist/` 目录到服务器
3. 配置服务器以支持 SPA 路由（如需要）

### Q: 如何添加环境变量

创建 `.env` 文件：
```
VITE_API_URL=https://api.example.com
```

在代码中使用：
```javascript
const apiUrl = import.meta.env.VITE_API_URL
```

## 🔗 相关文档

- [Vite 官方文档](https://vitejs.dev/)
- [Vite 配置参考](https://vitejs.dev/config/)
- [Vite 部署指南](https://vitejs.dev/guide/static-deploy.html)

## ✨ 优势

相比 Python 服务器：

| 特性 | Python | Vite |
|------|--------|------|
| 启动速度 | 慢 | ⚡ 快 |
| 热更新 | ❌ 无 | ✅ 有 |
| 生产优化 | 手动 | ✅ 自动 |
| 部署简单 | ❌ 需要 Python | ✅ 纯静态 |
| 开发体验 | 一般 | ⭐ 优秀 |

---

**现在您可以使用 `npm run dev` 开始开发了！** 🚀

